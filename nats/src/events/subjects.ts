// all of the possible subjects
// using enum which is similar to an object saying, if there is Subjects, then it has to be the one from the list
// saves a lot of type possibiliteis
export enum Subjects {
  ItemCreated = 'item:created',
  PurchaseUpdated = 'purchase:updated',
}

